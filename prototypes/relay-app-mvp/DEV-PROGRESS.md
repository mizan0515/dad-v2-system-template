## 2026-04-17 Session 1

### Completed
- Boot: read prototype state docs and verified baseline desktop build before new work.
- A7: relaxed the bounded prompt contract to marker-based handoff extraction and removed bounded CLI schema forcing.
- B4: added a dedicated `Latest Git / PR Activity` panel plus diagnostics and auto-log output.
- A2: added a broker-owned durable approval queue/history, wired interactive Codex approvals through it, and exposed an `Approval Queue` operator panel plus diagnostics output.
- G2 bridge: promoted unmanaged `mcp` / `web` activity from advisory-only into broker review items that can pause the session and be approved once or for the remainder of the session.
- A6 polish: surfaced matching saved session-approval rules directly in the pending approval and session-rule UI summaries.
- C1/G2: completed a real MCP capability audit for Codex and Claude, added `mcp-audit.md` / `capability-matrix.md`, and tightened MCP review policy so read-only resource discovery and telemetry ping/status auto-clear through broker policy while other MCP activity still pauses for review.
- C2: completed a real shell/PowerShell capability audit using the TaskPulse DAD workspace, captured live relay shell evidence plus direct Claude shell evidence, and recorded the results in `shell-audit.md` / `capability-matrix.md` / `TESTING-CHECKLIST.md`.
- C3: completed the read-only tier of the git workflow capability audit. Captured live WPF-driven relay evidence (session `git-audit-20260417-111226`) plus direct Codex and direct Claude git evidence against the TaskPulse workspace, and recorded everything in `git-audit.md` / `capability-matrix.md` / `TESTING-CHECKLIST.md`. Surfaced a real product gap: Codex wraps every Windows command in `powershell.exe -Command '...'`, so `RelayApprovalPolicy.ClassifyCommandCategory` never matches the `git*`/`pr` categories for Codex on Windows, and the destructive-tier git approval flow is therefore currently unreachable for Codex on Windows in practice.
- C3 follow-up (classifier fix): closed the Codex/Windows PowerShell-wrapping classifier gap. `RelayApprovalPolicy.ClassifyCommandCategory` now unwraps `"...\powershell.exe" -Command '<inner>'`, `pwsh -Command '<inner>'`, and `cmd /c <inner>` before matching on `git`/`git commit`/`git add`/`git push`/`gh pr create`, and strips `git -c key=value` / `git -C <path>` option pairs before subcommand matching. The Codex adapter now also refines `commandExecution` item categorization from the generic `shell` class to the more specific git/git-add/git-commit/git-push/pr class when the wrapped command warrants it.
- C3 destructive-tier live exercise: ran real `git add` / `git commit` / `git push` against a disposable `audit/destructive-20260417` branch of the TaskPulse workspace through the interactive relay (session `destructive-qa-20260417-131500`). Broker emitted `git.add.requested`/`.completed`, `git.commit.requested`/`.completed`, and `git.push.requested` + `approval.requested` + `approval.queue.enqueued` + `git.push.completed (declined)`. Codex-side sandbox runs `git add`/`git commit` directly without an approval round-trip, while `git push` correctly escalates to the broker as `item/commandExecution/requestApproval`. Surfaced a real product gap: `AutoApproveAllRequests=true` did not auto-resolve the server-originated push approval in the Codex interactive transport, so the push was denied by timeout and never reached the remote. Captured in `git-audit.md` and `capability-matrix.md`. `gh pr create` live exercise still pending (blocked on a real remote).
- Phase E opening survey (iteration 11): authored `phase-e-survey.md` mapping the Phase E deliverables (E1 tool-rich interactive contract / E2 failure-to-handoff recovery) to behavior already present in the prototype. E1 covered by the A7 marker-based handoff relaxation + multi-step live evidence (`git-classify-qa-20260417-115929`, `destructive-qa-20260417-131500`, `dad-asset-band-qa-20260417-190000`). E2 covered by the existing `RelayBroker` repair flow (`repair.requested`/`.completed`, `RunRepairAsync`, bounded-fallback downgrade in `TryDowngradeRepairAsync`). Surfaced the unproven exit-criteria leg (test runner inside one turn) and four recommended slices: E-live-1 (edit → test → handoff), E-live-2 (deliberate repair trigger), E-spec-1 (turn-budget signal), E-spec-2 (carry-partial-work into repair prompt). No source changes — docs only.
- Phase D status doc (iteration 10): authored `git-workflow.md` mapping the Phase D deliverables (D1 commit wrapper / D2 push wrapper / D3 PR wrapper / D4 safety defaults) to concrete evidence already landed during the Phase C audits and iterations 3/5/9. Consolidates classifier coverage, protected-branch detection, force-push deny logic, and outstanding gaps (commit SHA / branch / commit-range structured extraction, live `gh pr create`, msys/sh.exe sandbox fix). Exit criteria: classification + recording side substantively done; remaining work is verification + quality-of-life. No source changes — docs only.
- DAD-asset band (iteration 9): added a dedicated `dad-asset` category in `RelayApprovalPolicy` and wired Codex adapter fileChange refinement so writes under `Document/dialogue/**` are surfaced distinctly from generic file changes. `IsDadAssetPath` matches the DAD root, `RefineFileChangeCategory` promotes `file-change` → `dad-asset` when the target path qualifies, and `TargetsProtectedDadAssetPath` escalates backlog/state writes to `critical` risk. Policy-key, approval title (`DAD Asset Change Approval`), event stage (`dad.asset.<stage>`), and default-policy description all thread the new category. `CodexInteractiveAdapter` refines both the notification path (for auto-completed writes) and the approval path via a new `BuildFileChangePendingApproval` helper using `TryReadItemFileChangePath`. Verified live in WPF QA session `dad-asset-band-qa-20260417-190000`: 2× `dad.asset.requested`/`.completed` (Document/dialogue/notes/band-marker-1.md + band-marker-2.md), 1× `file.change.requested`/`.completed` (notes/outside-dad.md at repo root) — the classifier correctly separates DAD-internal from generic file writes.
- Read classifier (iteration 8): added a `read` category to `RelayApprovalPolicy.ClassifyCommandCategory` that matches `Get-Content`/`gc`/`cat`/`type`/`Select-String`/`sls` at the head of the unwrapped PowerShell payload, and rejects anything containing `|`, `;`, `>`, or `&` so piped/chained variants still fall through to `shell`. Threaded the new category through policy-key (`read:<cmd>`), approval title (`Read Approval`), risk level (`low`), event stage (`read.<stage>`), default-policy description (`auto-allowed once`), command summary, and the `ResolveDefaultDecision` auto-allow branch. Verified live in WPF-driven QA session `read-classify-qa-20260417-180000`: the pure `Get-Content 'PROJECT-RULES.md'` command emitted `read.requested`/`.completed`, while three compound variants (`Get-Content … | Select-Object`, `$content = Get-Content … if …`, `(Get-Content … -Raw).Length`) correctly stayed as `shell` — exactly the conservative stance we want.
- Codex Windows compatibility matrix: consolidated the shell/git/MCP/DAD live-session evidence into `codex-windows-matrix.md`. Captures the concrete Windows-specific Codex behaviors (PowerShell wrapping for all shell/reads/git commands, native `fileChange` for writes, silent directory creation, `git push` as the only escalated git subcommand, msys/sh.exe pipe failure under Job Object sandbox, `accept`/`cancel` decision names required in auto-approve). Rolled up `capability-matrix.md` — `Codex Windows compatibility matrix` flipped from Pending to Working.
- DAD asset classification audit: ran live WPF-driven QA session `dad-asset-qa-20260417-145500` against `D:\dad-relay-mvp-temp` (DAD-template workspace) with `AutoApproveAllRequests=true`. Prompt asked for (1) read `Document/dialogue/backlog.json`, (2) create `Document/dialogue/notes/audit-marker.md` via patch, (3) append to `Document/dialogue/README.md` via patch. Broker observed 1 `shell.requested`/`.completed` (the read wrapped as `powershell.exe -Command "Get-Content -Raw 'Document/dialogue/backlog.json'"`), 2 `file.change.requested`/`.completed` (one `add`, one `update`), `turn.completed` + `handoff.accepted`. No approval round-trip fired (auto-approve path). Findings captured in `dad-asset-audit.md` and rolled into `capability-matrix.md`. Two gaps surfaced: (a) no DAD-specific band in `RelayApprovalPolicy` — writes to `Document/dialogue/**` are treated identically to any other file change; (b) DAD-asset reads collapse into the generic `shell` category on Codex/Windows because Codex wraps reads in `powershell.exe -Command "Get-Content …"`. Also noticed Codex's patch fidelity deviated on the README append (appended literal `A` rather than the prompt's append string) — a Codex prompt-fidelity issue, not a broker bug.
- Auto-approve server-side fix: closed the `AutoApproveAllRequests` gap surfaced by the destructive-tier exercise. Two defects fixed in `CodexInteractiveAdapter`: (1) `BuildServerRequestResponse` now maps `ApproveForSession` → `accept` for `commandExecution` / `fileChange` approval methods (previously `acceptForSession` was emitted and treated by Codex as unknown → implicit decline); (2) `HandleServerRequestAsync` now resolves `autoApproveAll` synchronously in the adapter, emitting `approval.requested` + `approval.auto_mode.applied` + `approval.granted` into the observed-actions stream and firing the queue-persist work on a background task, eliminating the ~200ms UI-thread race that previously let Codex decline the command before our reply reached the socket. Verified live in QA session `auto-approve-push-qa-20260417-143000`: the `git push` command actually executed (exit 1 returned is an unrelated msys/sh.exe pipe-creation issue on this Windows sandbox, not an approval decline) and the turn completed with `handoff.accepted`.

### Build status
- pass 2026-04-17 initial boot build
- pass 2026-04-17T09:41:18+09:00 after bounded prompt/adapter changes
- pass 2026-04-17T09:45:45+09:00 after latest git/pr activity panel changes
- pass 2026-04-17T10:11:05+09:00 after broker-owned approval queue/history changes
- pass 2026-04-17T10:29:18+09:00 after MCP/web review-item bridge changes
- pass 2026-04-17T10:42:11+09:00 after active session-rule visibility UI updates
- pass 2026-04-17T10:15:53+09:00 after MCP audit follow-through and MCP default review policy tightening
- pass 2026-04-17T11:03:00+09:00 baseline build confirmed before starting the git workflow audit
- pass 2026-04-17T11:57:00+09:00 after Codex/Windows PowerShell-wrapping classifier fix and Codex adapter category refinement
- pass 2026-04-17T13:15:00+09:00 baseline build before destructive-tier live exercise (no source changes in this slice)
- pass 2026-04-17T14:35:00+09:00 after auto-approve server-side fix (Codex protocol decision mapping + adapter synchronous auto-approve path)
- pass 2026-04-17T15:00:00+09:00 baseline confirmed for DAD asset classification audit (no source changes in this slice — docs only)
- pass 2026-04-17T17:40:00+09:00 no source changes in this slice — Codex Windows compatibility matrix is synthesis-only (docs only)
- pass 2026-04-17T18:00:00+09:00 after read-category classifier addition (RelayApprovalPolicy)
- pass 2026-04-17T19:00:00+09:00 after dad-asset band addition (RelayApprovalPolicy + CodexInteractiveAdapter fileChange refinement)
- pass 2026-04-17T19:20:00+09:00 no source changes in this slice — Phase D status doc is synthesis-only
- pass 2026-04-17T20:05:00+09:00 no source changes in this slice — Phase E opening survey is synthesis-only

### Runtime verification
- Baseline build only. No new runtime verification yet in this session.
- `claude -p "Return exactly the word ok." --output-format json` returned success.
- `codex exec --json "Return exactly the word ok."` returned `ok` with expected warnings on stderr.
- `dotnet run --project .\RelayApp.Desktop\RelayApp.Desktop.csproj` launched successfully and was stopped after a short smoke start.
- `dotnet run --project .\RelayApp.Desktop\RelayApp.Desktop.csproj` launched successfully again after the WPF `Latest Git / PR Activity` panel was added.
- `dotnet run --project .\RelayApp.Desktop\RelayApp.Desktop.csproj` launched successfully again after the approval queue/history panel and broker queue wiring were added.
- `dotnet run --project .\RelayApp.Desktop\RelayApp.Desktop.csproj` launched successfully again after unmanaged MCP/web review-item handling was added.
- `dotnet run --project .\RelayApp.Desktop\RelayApp.Desktop.csproj` launched successfully again after active session-rule visibility UI updates.
- `claude -p "Return exactly the word ok." --output-format json` returned success after the MCP/web review bridge changes.
- `codex exec --json "Return exactly the word ok."` returned `ok` after the MCP/web review bridge changes.
- `codex mcp list` / `codex mcp get unityMCP` confirmed enabled Codex MCP config.
- `claude mcp list` / `claude mcp get UnityMCP` confirmed Claude MCP is workspace-dependent and available in `D:\Unity\card game`.
- Real Codex MCP turn succeeded with `manage_editor` + `telemetry_ping`.
- Real Claude MCP turn succeeded with direct `mcp__UnityMCP__manage_editor` telemetry ping and with MCP resource discovery.
- `dotnet run --project .\RelayApp.Desktop\RelayApp.Desktop.csproj` launched successfully again after MCP default review policy tightening.
- Root maintainer validator still fails only on the pre-existing `en` variant issue: `Variant document validation failed for D:\dad-v2-system-template\en`.
- Real WPF-driven QA succeeded for the shell audit session `shell-audit-20260417-102903`; the broker recorded `shell: completed=8, requested=8` and accepted the handoff to Claude.
- Direct Claude shell audit against `D:\dad-relay-mvp-temp` succeeded and returned `ok` after structured Bash-based repository inspection.
- Real WPF-driven git audit session `git-audit-20260417-111226` ran in INTERACTIVE mode, completed Codex Turn 1 with 11 `shell.requested`/11 `shell.completed` events (including the five `git ...` commands), and accepted the handoff to Claude with an accurate read-only summary.
- Direct Codex git audit (`codex exec --json --cd D:/dad-relay-mvp-temp ...`) returned `ok` and each wrapped `powershell.exe -Command 'git ...'` completed with exit 0.
- Direct Claude git audit (`claude -p ... --output-format stream-json`) returned `ok` using a single compound Bash call chaining the four read-only git commands.
- Post-fix WPF-driven QA session `git-classify-qa-20260417-115929` ran in INTERACTIVE mode, completed Codex Turn 1 with 5 `git.requested` / 5 `git.completed` events (the five wrapped `git ...` commands) alongside 4 `shell.requested` / 4 `shell.completed` events for the remaining non-git PowerShell commands. The `Latest Git / PR Activity` panel correctly surfaced the git events for the first time on Codex/Windows.
- DAD-asset band WPF-driven QA session `dad-asset-band-qa-20260417-190000` ran in INTERACTIVE mode against `D:\dad-relay-mvp-temp` with `AutoApproveAllRequests=true`. Broker observed 2× `dad.asset.requested`/`.completed` (Document/dialogue/notes/band-marker-1.md + band-marker-2.md) and 1× `file.change.requested`/`.completed` (repo-root notes/outside-dad.md), `turn.completed` + `handoff.accepted`. Files verified on disk. Confirms DAD-internal writes are now surfaced distinctly while non-DAD writes correctly stay in the generic `file-change` band.
- Read-classifier WPF-driven QA session `read-classify-qa-20260417-180000` ran in INTERACTIVE mode against `D:\dad-relay-mvp-temp` with `AutoApproveAllRequests=true`. Broker observed 1 `read.requested`/`.completed` (Codex bootstrap read of `PROJECT-RULES.md`: `Get-Content -Path 'PROJECT-RULES.md' -TotalCount 120`) and 3 `shell.requested`/`.completed` for the compound variants from the prompt (`Get-Content … | Select-Object`, `$content = Get-Content … if …`, `(Get-Content … -Raw).Length`). `turn.completed` + `handoff.accepted`. Confirms the classifier's intended conservative stance: pure `Get-Content <path>` is `read` and auto-allowed, anything piped/scripted remains `shell` and operator-approved.
- DAD asset classification WPF-driven QA session `dad-asset-qa-20260417-145500` ran in INTERACTIVE mode on `D:\dad-relay-mvp-temp` with `AutoApproveAllRequests=true`. Broker observed 1 `shell.requested`/`.completed` (Codex wrapped the `backlog.json` read as `powershell.exe -Command "Get-Content -Raw ..."`), 2 `file.change.requested`/`.completed` (`notes/audit-marker.md` add + `README.md` update), `turn.completed` + `handoff.accepted`. No approval round-trip — both `fileChange` items auto-completed under the iteration-5 auto-approve fix. Files verified on disk. Two real product gaps surfaced and recorded in `dad-asset-audit.md`: DAD paths have no dedicated band in `RelayApprovalPolicy`, and DAD reads collapse into the generic `shell` category on Codex/Windows.
- Destructive-tier WPF-driven QA session `destructive-qa-20260417-131500` ran in INTERACTIVE mode on disposable branch `audit/destructive-20260417` of the TaskPulse workspace with `AutoApproveAllRequests=true`. Broker observed `git.add.requested`/`.completed` (exit 0), `git.commit.requested`/`.completed`, `git.push.requested` + `approval.requested` + `approval.queue.enqueued` + `git.push.completed (declined)`. `git add` and `git commit` ran inside Codex's sandbox without routing approvals to the broker; `git push` correctly escalated as `item/commandExecution/requestApproval` and was denied by timeout because `AutoApproveAllRequests` did not auto-resolve the server-originated approval.

### Next priority
- E-live-1: edit → test → handoff QA against TaskPulse workspace (single-turn pytest exercise) — closes the unproven leg of the Phase E exit criteria.
- E-live-2: deliberate repair-flow trigger on the interactive transport to capture live `repair.requested`/`.completed` evidence.
- Live `gh pr create` exercise against a real GitHub remote (currently blocked on a real remote).
- Direct Claude parity pass for the destructive git tier.
- Non-blocking: msys/sh.exe pipe-creation failure under Job Object sandbox (iteration 5 finding).
- Live `gh pr create` destructive-tier exercise (still blocked on a real GitHub remote).
- Direct Claude parity pass for the destructive git tier.
- Non-blocking: investigate the Windows msys/sh.exe pipe-creation failure under the Job Object sandbox surfaced in `auto-approve-push-qa-20260417-143000`. Not blocking approval flow; relevant for any git operation that forks an sh.exe child inside the sandbox.

### Loop status
- 2026-04-17 Session 1 paused after C3 read-only merge (PR #5). Context budget approaching the agreed threshold; resuming the loop on the next scheduled wake-up.
- 2026-04-17 Session 1 iteration 2: classifier fix shipped as PR #TBD; pausing after merge. Next iteration will take on the destructive-tier live exercise.
- 2026-04-17 Session 1 iteration 3: 컨텍스트 포화로 일시 정지. 이전 대화 요약으로 재진입했으나 C3 destructive-tier 라이브 실행(분리 브랜치 준비 + WPF UI Automation QA + JSONL 이벤트 검증)은 한 번의 턴에 안전하게 들어가지 않음. 다음 반복에서 `dev/git-destructive-live` 브랜치로 새로 시작 예정.
- 2026-04-17 Session 1 iteration 4: C3 destructive-tier add/commit/push 라이브 실행 완료. 다음 반복은 `AutoApproveAllRequests` 서버측 승인 자동 수락 구현으로 진행 예정.
- 2026-04-17 Session 1 iteration 5: `AutoApproveAllRequests` 서버측 자동 승인 수정 완료 (adapter 동기 처리 + Codex 프로토콜 결정 이름 수정). 다음 반복은 DAD asset classification 감사로 진행 예정.
- 2026-04-17 Session 1 iteration 6: DAD asset classification 감사 완료 (`dad-asset-audit.md`). 라이브 QA 세션 `dad-asset-qa-20260417-145500`로 DAD 경로의 write/read 분류 실증. 주요 gap 2건 기록: DAD 전용 밴드 부재 + Codex/Windows에서 read가 shell로 붕괴. 컨텍스트 포화 임박으로 일시 정지; 다음 반복은 Codex Windows compatibility matrix로 진행 예정.
- 2026-04-17 Session 1 iteration 7: Codex Windows compatibility matrix 완료 (`codex-windows-matrix.md`). 기존 shell/git/MCP/DAD 라이브 세션 증거를 Windows 관점으로 통합. 후속 반복은 classifier 보강(Get-Content 인식 + DAD-asset 밴드) 또는 Post-MVP 생산 문서 작성으로 진행 예정.
- 2026-04-17 Session 1 iteration 8: read-category classifier 추가 (`RelayApprovalPolicy`). 라이브 QA 세션 `read-classify-qa-20260417-180000`로 pure Get-Content은 `read`, 파이프/스크립트는 `shell`로 분리되는 것을 확증. 후속 반복은 DAD-asset 밴드 추가 또는 `gh pr create` 라이브 연습(원격 필요) 또는 Post-MVP 생산 문서로 진행 예정.
- 2026-04-17 Session 1 iteration 9: DAD-asset band 추가 (`RelayApprovalPolicy` + `CodexInteractiveAdapter` fileChange 세분화). 라이브 QA 세션 `dad-asset-band-qa-20260417-190000`로 Document/dialogue/** 쓰기는 `dad.asset.*`로, 그 외 쓰기는 `file.change.*`로 분리되는 것을 확증. Phase C의 classifier follow-up 모두 완료. 후속 반복은 Post-MVP 생산 문서 작성 또는 보류 항목(real-remote `gh pr create`, 직접 Claude git 패러티) 진행 예정.
- 2026-04-17 Session 1 iteration 10: Phase D 상태 문서(`git-workflow.md`) 작성 완료. D1~D4 deliverable을 기존 증거에 매핑하고 남은 gap을 정리. Phase D는 분류/기록 관점에서 실질적으로 완료. 후속 반복은 Phase E(Deep Tool-Chain Runtime) 오프닝 서베이로 진행 예정.

### Blockers or decisions needed
- Claude remains audit-only by design; no blocker for this session, but full approval parity still depends on a later product decision.
- Root source-repo validator still fails on the pre-existing `en` variant maintainer path and is unrelated to prototype changes.
- The shell audit exposed a likely UI refresh timing issue during long-running live turns; final state was correct, but near-real-time state refresh should be re-checked in a follow-up QA slice before treating it as a confirmed product bug.
- ~~Codex-on-Windows wraps every command in `powershell.exe -Command '...'`, so the current `RelayApprovalPolicy.ClassifyCommandCategory` does not route git/pr work through the dedicated `git-add`/`git-commit`/`git-push`/`pr` approval classes.~~ (resolved — classifier now unwraps shell wrappers and adapter refines command-execution items from `shell` to the specific git category; verified live in QA session `git-classify-qa-20260417-115929`).
