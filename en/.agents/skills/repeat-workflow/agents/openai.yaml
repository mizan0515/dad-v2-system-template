interface:
  display_name: "DAD v2 Repeat Workflow"
  short_description: "Execute the next turn of the active dialogue session"
  default_prompt: "Use $repeat-workflow to execute the next turn of the active DAD v2 session and emit the next handoff prompt."

policy:
  allow_implicit_invocation: false
