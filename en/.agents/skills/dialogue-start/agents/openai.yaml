interface:
  display_name: "DAD v2 Dialogue Start"
  short_description: "Start a symmetric-turn collaboration session with Claude Code (Codex takes Turn 1)"
  default_prompt: "Use $dialogue-start to start a DAD v2 session and produce Turn 1 plus the Claude Code handoff prompt."

policy:
  allow_implicit_invocation: false
